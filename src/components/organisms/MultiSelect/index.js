import React from 'react';
import { Multiselect as MultiselectLib } from 'multiselect-react-dropdown';

const MultiSelect = (props) => (
  <MultiselectLib {...props} />
);

export default MultiSelect;
